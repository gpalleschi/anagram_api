// Constants
const DEFAULT_PORT = 35907; 
const DB_PATH = './db/';

// Languages
const LANGUAGES = ['it','en','es'];

module.exports = {
    DEFAULT_PORT: DEFAULT_PORT,
    LANGUAGES: LANGUAGES,
    DB_PATH: DB_PATH,
};
